#include <string>
#include <vector>

using namespace std;

int solution(vector<vector<int>> dots)
{
    int x, y = 0;
    x = dots[0][0];
    y = dots[0][1];
    for (int i = 0; i < dots.size(); i++)
    {
    }
}